<lnk:WindowBase xmlns:lnk="clr-namespace:CI.GUI.Support.WpfLibrary.Base;assembly=CI.GUI.Support.WpfLibrary" x:Name="wnd" 
                x:Class="AsyncSocketTester.TesterMainWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d"
                Style="{DynamicResource WindowStyle_Aav0}"
                Title="Socket Explorer" Height="450" Width="800" Loaded="onLoaded">
  <WindowChrome.WindowChrome>
    <WindowChrome CaptionHeight="0" ResizeBorderThickness="10" />
  </WindowChrome.WindowChrome>
  <lnk:WindowBase.Resources>
    <ResourceDictionary>
      <Style TargetType="{x:Type StackPanel}">
        <Setter Property="Background" Value="#8888" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="Margin" Value="5" />
      </Style>
      <Style TargetType="{x:Type Button}">
        <Setter Property="Margin" Value="5" />
      </Style>
      <Style TargetType="{x:Type CheckBox}">
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Margin" Value="5" />
      </Style>
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/CI.GUI.Support.WpfLibrary;component/Base/BasicDefaultsLayout.xaml" />
        <!--
        <ResourceDictionary Source="/CI.GUI.Support.WpfLibrary;component/Base/DataGrid.Base.xaml" />
        <ResourceDictionary Source="/CI.GUI.Support.WpfLibrary;component/ColorScheme/Theme.Color.Dark.Gray.xaml" />-->
      </ResourceDictionary.MergedDictionaries>

      <!--<Style TargetType="{x:Type stb:HighlightableTextBlock}">
        <Setter Property="ToolTipService.ShowDuration" Value="999999" />
        <Setter Property="HighlightForeground" Value="#c00" />
        <Setter Property="HighlightBackground" Value="#fcc" />
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="{x:Type stb:HighlightableTextBlock}">
              <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                <TextBlock x:Name="PART_TEXT"/>
              </Border>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>

      <CollectionViewSource x:Key="cmdViewSource" d:DesignSource="{d:DesignInstance {x:Type Models:Cmd}, CreateList=True}"/>
      <CollectionViewSource x:Key="cmdLogsViewSource" Source="{Binding Logs, Source={StaticResource cmdViewSource}}"/>-->
    </ResourceDictionary>
  </lnk:WindowBase.Resources>
  <Border CornerRadius="9" Background="#def" BorderBrush="#06d" BorderThickness="2">
    <Grid>
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="4*"/>
        <ColumnDefinition Width="10"/>
        <ColumnDefinition Width="5*"/>
      </Grid.ColumnDefinitions>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*"/>
        <RowDefinition Height="Auto"/>
      </Grid.RowDefinitions>

      <Image Source="/internet_connections_3.ico" HorizontalAlignment="Left" Margin="10" Height="32" />

      <TextBlock Grid.Column="0" Margin="5" Grid.Row="1" TextWrapping="Wrap" x:Name="tbkReportClt" VerticalAlignment="Top" Foreground="#070" d:Text="result of server activities - line 1 - line 1 - line 1 - line 1 - line 1" FontFamily="JetBrains Mono"/>
      <TextBlock Grid.Column="2" Margin="5" Grid.Row="1" TextWrapping="Wrap" x:Name="tbkReportSvr" VerticalAlignment="Top" Foreground="#00d" d:Text="result of server activities - line 1 - line 1 - line 1 - line 1 - line 1" FontFamily="JetBrains Mono" />

      <Rectangle Grid.Column="0" Margin="5" Grid.Row="0" d:Fill="Orange" VerticalAlignment="Center" HorizontalAlignment="Right" Width="32" Height="32" x:Name="ra"/>
      <Rectangle Grid.Column="2" Margin="5" Grid.Row="0" d:Fill="Orange" VerticalAlignment="Center" HorizontalAlignment="Right" Width="32" Height="32" x:Name="rb"/>

      <StackPanel Grid.Row="2" Grid.Column="2" Orientation="Horizontal">
        <CheckBox Content="_Server ON" HorizontalAlignment="Left" Margin="5" VerticalAlignment="Center" Checked="onToggleSvrListening" x:Name="chkSvr"/>
        <Button Content="_Read Report" Click="onRR" />
      </StackPanel>

      <StackPanel Grid.Row="2" Orientation="Horizontal">
        <Label Content="   Call: " />
        <Button Content="_Strng to Lcl" Click="onMe" x:Name="b1" IsEnabled="False"/>
        <Button Content="Login to .152" Click="onRealHere" />
        <Button Content="Login to .152" Click="onRealReal" />
      </StackPanel>

      <GridSplitter Grid.Column="1" Grid.RowSpan="3" Background="AliceBlue" />

    </Grid>
  </Border>
</lnk:WindowBase>
