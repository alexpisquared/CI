<Window x:Class="CI.DS.Visual.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CI.DS.Visual.Views"
        xmlns:vm="clr-namespace:CI.DS.ViewModel;assembly=CI.DS.ViewModel"
        mc:Ignorable="d"
        Title="MainView" Height="450" Width="800">
  <Window.DataContext>
    <vm:DemoViewModel/>
  </Window.DataContext>
  <StackPanel>
    <TextBox Text="{Binding StringMayNotBeEmpty, UpdateSourceTrigger=PropertyChanged}" />
    <local:AllCashByGroupView Height="360"/>
  </StackPanel>
</Window>
