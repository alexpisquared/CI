<UserControl x:Class="CI.DS.Visual.Views.DbProcessSelectorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CI.DS.Visual.Views"
             mc:Ignorable="d" 
             d:DesignHeight="350" d:DesignWidth="800" Background="{DynamicResource MenuItemBackground0}">
  <UserControl.Resources>
    <Style TargetType="{x:Type Label}" BasedOn="{StaticResource LblBase}">
      <Setter Property="HorizontalAlignment"  Value="Left" />
    </Style>
  </UserControl.Resources>
  <Grid>
    <StackPanel Margin="30 10" >
      <TextBlock Text="DB Process Selector" FontSize="26" Margin="0 20" />
      
      <Label Content="Search" />
      <TextBox Text="{Binding StringMayNotBeEmpty, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Stretch" />
      
      <Label Content="DB Processes"    />
      <ListBox ItemsSource="{Binding DbProcesses}" DisplayMemberPath="Name" SelectedValuePath="PKey" SelectedValue="{Binding PKey}" />

      <Label Content="Selected DB Process:" />
      <TextBlock Text="{Binding PKey}"  d:Text="Milky Way" FontSize="18" />

      <Button Content="_Run" IsDefault="True" HorizontalAlignment="Center" />
    </StackPanel>

  </Grid>
</UserControl>
