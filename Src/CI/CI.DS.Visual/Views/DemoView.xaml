<UserControl x:Class="CI.DS.Visual.Views.DemoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CI.DS.Visual.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="#505">
  <Grid>
    <StackPanel Margin="20 10" >
      <TextBlock Text="Demo Vew - ViewModel" FontSize="26" Foreground="#f0f" Margin="20 10" />
      
      <Label Margin="10 20 0 0" Content="String May Not be Empty:" Foreground="#ccc" />
      <TextBox Text="{Binding StringMayNotBeEmpty, UpdateSourceTrigger=PropertyChanged}" Margin="20 0" />
      
      <Label Margin="10 20 0 0" Content="Where am I:"      Foreground="#ccc" />
      <TextBlock Text="{Binding WhereAmI}" Margin="20 0"   Foreground="#eee" d:Text="Milky Way" FontSize="18" />
      
      <Label Margin="10 20 0 0" Content="SQL Conn String:" Foreground="#ccc" />
      <TextBlock Text="{Binding SqlConStr}" Margin="20 0"  Foreground="#eee" d:Text="Milky Way" FontSize="18" />
    </StackPanel>

  </Grid>
</UserControl>
